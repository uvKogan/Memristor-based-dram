[pytest]
# The `slow` marker is registered in tests/conftest.py.  Slow tests are
# deselected by default so that `python3 -m pytest tests -q` stays quick;
# run them with `-m slow`, or run everything with `-m ""`.
addopts = -m "not slow"
